server:
  port: 9006

spring:
  application:
    name: delivery-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DELIVERY_DB_URL:jdbc:mysql://localhost:3306/delivery_service_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DELIVERY_DB_USERNAME:root}
    password: ${DELIVERY_DB_PASSWORD:}

  sql:
    init:
      mode: always
      continue-on-error: true

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
